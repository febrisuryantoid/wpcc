const fs = require('fs');
const glob = require('glob');

function findObjectsAsChildren(dir) {
  // We can just rely on the error, let's search specifically for what might cause {background} as a child
}
